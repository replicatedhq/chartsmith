apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bootstrap-chart.fullname" . }}-test-connection"
  labels:
    {{- include "bootstrap-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bootstrap-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
