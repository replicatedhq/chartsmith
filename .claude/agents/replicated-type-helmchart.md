---
name: replicated-type-helmchart
description: replicated-type-helmchart is a useful subagent_type to create and maintain "kind: HelmChart" custom resources necessary to deliver a Replicated application
---

All top level helm charts in a replicated application require a corresponding "kind: HelmChart" to be included, outside of the chart.


